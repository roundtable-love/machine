#! /usr/bin/env nix-shell
#! nix-shell -i python3 --package "python3.withPackages(pkgs: with pkgs; [typer])"

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parents[1] / "src"))

from machine.schema import app

app()

# vim:ft=python
